const { app } = require('egg-mock/bootstrap');

module.exports = app => {
    app.beforeStart(async () => {

    });

    app.ready(async () => {

    });

    app.beforeClose(async () => {

    });
};